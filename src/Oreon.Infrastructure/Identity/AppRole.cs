using Microsoft.AspNetCore.Identity;

namespace Oreon.Infrastructure.Identity;

public sealed class AppRole : IdentityRole<Guid>
{
    // Identity technical properties are inherited from IdentityRole<Guid>
}
